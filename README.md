# node-graphql-api-server

node-graphql-api-server is GraphQL API Server from graphql-yoga.

## Usage

```sh
$ git clone https://github.com/ruinbible/node-graphql-api-server.git

$ yarn install

$ yarn start
```
